<!DOCTYPE html>
<html>
<head>
  <title>KeymailExample</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">

  <header>
    <h1>Keymail example</h1>
    <p>This is an example application that demonstrates <%= link_to 'Keymail', 'https://github.com/alcesleo/keymail' %>, here is the <%= link_to 'source', 'https://github.com/alcesleo/keymail-example' %>.</p>
  </header>

  <% flash.each do |type, message| %>
    <div class="<%= flash_class(type) %>"><%= message %></div>
  <% end %>

  <div class="user-info">
    <% if current_user %>
      <p>
        You are currently logged in as <strong><%= current_user.email %></strong>. Type a message below or
        <%= link_to 'Log out', log_out_path, class: 'btn btn-danger btn-xs' %>.
      </p>
    <% elsif %>
      <p>Please log in by entering your email and pressing <strong>enter</strong>, then click the link that will be emailed to you.</p>
    <% end %>
  </div>

  <% unless current_user %>
    <%= render 'shared/authentication_form' %>
  <% end %>

  <%= yield %>
</div>

</body>
</html>
