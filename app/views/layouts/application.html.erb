<!DOCTYPE html>
<html>
<head>
  <title>KeymailExample</title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">
  <h1>Keymail example</h1>
  <p>This is an example application that demonstrates Keymail.</p>

  <%= link_to 'Log out', log_out_path if current_user %>

  <% flash.each do |type, message| %>
    <div class="<%= flash_class(type) %>"><%= message %></div>
  <% end %>

  <% unless current_user %>
    <%= render 'shared/authentication_form' %>
  <% end %>

  <%= yield %>
</div>

</body>
</html>
