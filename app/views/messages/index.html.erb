<%= render 'form' %>

<% @messages.each do |message| %>
    <div>
        <p><%= message.text %></p>
        <aside>
            By <%= message.user_email %>
            at <%= time_tag(message.created_at) %>
        </aside>
    </div>
<% end %>

<%= paginate @messages %>
